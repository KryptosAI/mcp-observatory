{
  "name": "@kryptosai/mcp-observatory-api",
  "version": "0.1.0",
  "private": true,
  "description": "Hosted Scan API for MCP Observatory — Cloudflare Worker",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260621.1",
    "typescript": "^5.7.0",
    "wrangler": "^4.103.0"
  },
  "overrides": {
    "esbuild": "^0.28.1",
    "ws": "^8.21.0"
  }
}
